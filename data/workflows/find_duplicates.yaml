# Workflow: Find Duplicates
# =========================
# Walk a directory, compute checksums, and find duplicate files.

workflow: "find_duplicates"
inputs:
  path: "~/Documents"
steps:
  - walk_tree
  - filter:
      extension: "*"
  - sort_by: name
