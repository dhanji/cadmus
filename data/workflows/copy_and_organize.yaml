# Workflow: Copy and Organize
# ===========================
# Copy files matching a pattern to a new directory, then sort by name.

workflow: "copy_and_organize"
inputs:
  source: "~/Downloads"
  dest: "/tmp/organized"
  pattern: "*.pdf"
steps:
  - walk_tree
  - filter:
      extension: ".pdf"
  - sort_by: name
