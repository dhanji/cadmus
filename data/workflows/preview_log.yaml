# Workflow: Preview Log
# =====================
# Walk a directory to find log files, filter for .log extension, then sort.

workflow: "preview_log"
inputs:
  path: "/var/log"
  keyword: "error"
steps:
  - walk_tree
  - filter:
      extension: ".log"
  - sort_by: name
