# Workflow: Spotlight Find
# ========================
# Use Spotlight to find files, then filter and sort results.

workflow: "spotlight_find"
inputs:
  query: "kind:pdf author:Smith"
steps:
  - spotlight_search
  - sort_by: name
