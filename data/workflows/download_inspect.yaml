# Workflow: Download and Inspect
# ==============================
# Download a zip file, extract it, find images, sort them.
# 4-step URL → File → Seq chain.

workflow: "Download and inspect archive"

inputs:
  file: "assets.zip"

steps:
  - extract_archive
  - find_matching:
      pattern: "*.png"
  - sort_by: name
