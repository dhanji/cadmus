# Plan: Cleanup Temp
# ======================
# Find old temporary files and list them for review.

cleanup-temp:
  inputs:
    - path
  steps:
    - walk_tree
    - filter:
        pattern: "*.tmp"
    - sort_by: name
