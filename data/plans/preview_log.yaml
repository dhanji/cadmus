# Plan: Preview Log
# =====================
# Walk a directory to find log files, filter for .log extension, then sort.

preview-log:
  inputs:
    - path
    - keyword
  steps:
    - walk_tree
    - filter:
        extension: ".log"
    - sort_by: name
