# Plan: Git Log Search
# ========================
# Search git commit history for a pattern.
# Uses power_tools ops: git_log → filter → sort_by.

git_log_search:
  inputs:
    - repo: Repo
    - pattern: Pattern
  steps:
    - git_log
    - filter:
        pattern: "$pattern"
    - sort_by: date
