repack_comics:
  inputs:
    - path: Dir
  steps:
    - list_dir
    - find_matching:
        pattern: "*.cbz"
    - sort_by: name
    - extract_archive: each
    - pack_archive:
        output: combined.cbz
