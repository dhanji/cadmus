# Plan: Find Duplicates
# =========================
# Walk a directory, compute checksums, and find duplicate files.

find_duplicates:
  inputs:
    - path: Dir
  steps:
    - walk_tree
    - filter:
        extension: "*"
    - sort_by: name
