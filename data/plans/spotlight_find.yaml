# Plan: Spotlight Find
# ========================
# Use Spotlight to find files, then filter and sort results.

spotlight-find:
  inputs:
    - query
  steps:
    - spotlight_search
    - sort_by: name
