# Variable-length quantity encoding
# Example: vlq(300) = (2 44) — 300 = 0x12C → groups of 7 bits
# expected: (172 2)

vlq_encoding:
  inputs:
    - n: "Number"
  bindings:
    n: "300"
  steps:
    - vlq_encoding
