# Triangular number: T(n) = n*(n+1)/2
# Example: T(10) = 55
# expected: 55

triangular_number:
  inputs:
    - n: "Number"
  bindings:
    n: "10"
  steps:
    - for_sum:
        var: "i"
        sequence: "(in-range 1 (+ n 1))"
        body: "i"
