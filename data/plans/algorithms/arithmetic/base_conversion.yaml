# Base conversion: convert decimal to given base
# Example: 255 in base 16 = "FF"
# expected: FF

base_conversion:
  inputs:
    - n: "Number"
    - base: "Number"
  bindings:
    n: "255"
    base: "16"
  steps:
    - base_conversion
