# Ackermann function: A(m,n) — fast-growing recursive function
# Example: A(3,2) = 29
# expected: 29

ackermann:
  inputs:
    - m: "Number"
    - n: "Number"
  bindings:
    m: "3"
    n: "2"
  steps:
    - ackermann
