# Factorial: n! = n × (n-1) × ... × 1
# Example: 10! = 3628800
# expected: 3628800

factorial:
  inputs:
    - n: "Number"
  bindings:
    n: "10"
  steps:
    - for_product:
        var: "i"
        sequence: "(in-range 1 (+ n 1))"
        body: "i"
