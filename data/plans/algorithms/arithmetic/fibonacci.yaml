# Fibonacci: F(n) where F(0)=0, F(1)=1, F(n)=F(n-1)+F(n-2)
# Example: F(10) = 55
# expected: 55

fibonacci:
  inputs:
    - n: "Number"
  bindings:
    n: "10"
  steps:
    - iterate:
        name: "loop"
        bindings: "a=0 b=1 i=0"
        body: "(if (= i n) a (loop b (+ a b) (+ i 1)))"
