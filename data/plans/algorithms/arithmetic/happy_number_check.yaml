# Happy number check: repeatedly sum squares of digits until 1 or cycle
# Example: 19 → 82 → 68 → 100 → 1 (happy!)
# expected: #t

happy_number_check:
  inputs:
    - n: "Number"
  bindings:
    n: "19"
  steps:
    - happy_number_check
