# CSV row parsing: split a comma-separated row into fields
# Example: csv_parse_row("a,b,c") = (a b c)
# expected: (a b c)

csv_parse_row:
  inputs:
    - row: "String"
  bindings:
    row: '"a,b,c"'
  steps:
    - csv_parse_row
