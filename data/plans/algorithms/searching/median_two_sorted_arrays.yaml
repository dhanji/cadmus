# Median of two sorted arrays
# Example: median of (1 3) and (2) = 2
# expected: 2

median_two_sorted_arrays:
  inputs:
    - a: "List(Number)"
    - b: "List(Number)"
  bindings:
    a: '(list 1 3)'
    b: '(list 2)'
  steps:
    - median_two_sorted_arrays
