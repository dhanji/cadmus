# Unique paths in m×n grid: top-left to bottom-right, only right/down
# Example: 3×7 grid = 28 paths
# expected: 28

unique_paths_grid:
  inputs:
    - m: "Number"
    - n: "Number"
  bindings:
    m: "3"
    n: "7"
  steps:
    - unique_paths_grid
