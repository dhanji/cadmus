;; Euclidean distance between two 2D points
;; Example: distance from (0,0) to (3,4) = 5.0
;; expected: 5

(define (euclidean_distance (x1 : Number) (y1 : Number) (x2 : Number) (y2 : Number))
  (bind x1 0)
  (bind x2 3)
  (bind y1 0)
  (bind y2 4)
  (euclidean_distance)
)
