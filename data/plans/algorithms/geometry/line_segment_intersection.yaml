# Line segment intersection: do segments (0,0)-(2,2) and (0,2)-(2,0) intersect?
# Example: intersect? = #t
# expected: #t

line_segment_intersection:
  inputs:
    - n: "Number"
  bindings:
    n: "0"
  steps:
    - line_segment_intersection
