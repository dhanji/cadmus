# Power of two check: n > 0 and n & (n-1) = 0
# Example: is_pow2(64) = #t
# expected: #t

power_of_two_check:
  inputs:
    - n: "Number"
  bindings:
    n: "64"
  steps:
    - if_then:
        test: "(and (> n 0) (= (bitwise-and n (- n 1)) 0))"
        then: "#t"
        else: "#f"
