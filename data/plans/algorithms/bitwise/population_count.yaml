# Population count: number of 1-bits in binary representation
# Example: popcount(255) = 8
# expected: 8

population_count:
  inputs:
    - n: "Number"
  bindings:
    n: "255"
  steps:
    - population_count
