# XOR sum of range 0..n
# Example: xor_sum(10) = 11 (0^1^2^...^10)
# expected: 11

xor_sum:
  inputs:
    - n: "Number"
  bindings:
    n: "10"
  steps:
    - for_fold:
        bindings: "[acc 0]"
        var: "i"
        sequence: "(in-range (+ n 1))"
        body: "(bitwise-xor acc i)"
