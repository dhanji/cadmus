# Hamming weight: count set bits using Brian Kernighan's method
# Example: 255 has 8 set bits
# expected: 8

hamming_weight:
  inputs:
    - n: "Number"
  bindings:
    n: "255"
  steps:
    - hamming_weight
