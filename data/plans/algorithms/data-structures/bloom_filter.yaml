# Bloom filter: probabilistic set membership (simplified)
# Insert "hello" "world", test "hello" "foo" → (#t #f)
# expected: (#t #f)

bloom_filter:
  inputs:
    - n: "Number"
  bindings:
    n: "0"
  steps:
    - bloom_filter
