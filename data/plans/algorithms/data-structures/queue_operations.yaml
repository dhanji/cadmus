# Queue operations: simulate a FIFO queue
# Example: enqueue 1,2,3, dequeue, dequeue → (1 2)
# expected: (1 2)

queue_operations:
  inputs:
    - ops: "List(Number)"
  bindings:
    ops: "(list 1 2 3 0 0)"
  steps:
    - queue_operations
