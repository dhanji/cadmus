# Max stack: stack with maximum query support
# Example: push 3, push 5, max query, pop, max query → (5 3)
# expected: (5 3)

max_stack:
  inputs:
    - ops: "List(Number)"
  bindings:
    ops: "(list 3 5 -1 0 -1)"
  steps:
    - max_stack
