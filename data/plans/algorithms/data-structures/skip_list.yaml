# Skip list: insert elements and check membership
# Insert 3 6 7 9 12, check 7 and 5
# expected: (#t #f)

skip_list:
  inputs:
    - n: "Number"
  bindings:
    n: "0"
  steps:
    - let_bind:
        bindings: "[elements (list 3 6 7 9 12)] [sorted (sort (list 3 6 7 9 12) <)]"
        body: "(list (if (member 7 sorted) #t #f) (if (member 5 sorted) #t #f))"
