# Euler's totient: count integers 1..n coprime to n
# Example: phi(12) = 4 (1, 5, 7, 11)
# expected: 4

euler_totient:
  inputs:
    - n: "Number"
  bindings:
    n: "12"
  steps:
    - for_sum:
        var: "i"
        sequence: "(in-range 1 n)"
        body: "(if (= (gcd i n) 1) 1 0)"
