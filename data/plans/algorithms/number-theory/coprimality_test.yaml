# Coprimality test: gcd(a,b) = 1?
# Example: coprime(15, 28) = #t
# expected: #t

coprimality_test:
  inputs:
    - a: "Number"
    - b: "Number"
  bindings:
    a: "15"
    b: "28"
  steps:
    - gcd:
        x: "$a"
        y: "$b"
    - equal:
        x: "$step-1"
        y: "1"
