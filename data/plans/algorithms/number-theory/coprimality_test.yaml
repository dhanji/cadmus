# Coprimality test: gcd(a,b) = 1?
# Example: coprime(15, 28) = #t
# expected: #t

coprimality_test:
  inputs:
    - a: "Number"
    - b: "Number"
  bindings:
    a: "15"
    b: "28"
  steps:
    - equal:
        x: "(gcd a b)"
        y: "1"
