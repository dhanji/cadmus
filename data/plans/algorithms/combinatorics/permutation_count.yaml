# Permutation count: P(n,k) = n! / (n-k)!
# Example: P(5, 3) = 60
# expected: 60

permutation_count:
  inputs:
    - n: "Number"
    - k: "Number"
  bindings:
    n: "5"
    k: "3"
  steps:
    - for_product:
        var: "i"
        sequence: "(in-range (- n k) n)"
        body: "(+ i 1)"
