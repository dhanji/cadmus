# Run-length encoding: compress consecutive chars
# Example: rle("aaabbc") = "3a2b1c"
# expected: 3a2b1c

run_length_encoding:
  inputs:
    - s: "String"
  bindings:
    s: '"aaabbc"'
  steps:
    - run_length_encoding
