# Palindrome check: is string a palindrome?
# Example: "racecar" = #t
# expected: #t

palindrome_check:
  inputs:
    - s: "String"
  bindings:
    s: "\"racecar\""
  steps:
    - let_bind:
        bindings: "[rev (list->string (reverse (string->list s)))]"
        body: "(string=? s rev)"
