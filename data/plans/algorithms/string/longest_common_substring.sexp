;; Longest common substring length
;; Example: lcstr("ABABC", "BABCBA") = 4 ("BABC")
;; expected: 4

(define (longest_common_substring (s1 : String) (s2 : String))
  (bind s1 "ABABC")
  (bind s2 "BABCBA")
  (longest_common_substring)
)
