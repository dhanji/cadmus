;; Anagram check: do two strings have the same character frequencies?
;; Example: "listen" and "silent" → true
;; expected: #t

(define (is_anagram (a : String) (b : String))
  (bind a ""listen"")
  (bind b ""silent"")
  (is_anagram)
)
