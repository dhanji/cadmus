# Plan: Copy and Organize
# ===========================
# Copy files matching a pattern to a new directory, then sort by name.

copy_and_organize:
  inputs:
    - source: Dir
    - dest: Dir
    - pattern: Pattern
  steps:
    - walk_tree
    - filter:
        extension: .pdf
    - sort_by: name
